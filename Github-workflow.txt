1-- $ git clone [repository]
//creates repository in folder
2-- $ git status
//shows status of files in repository
3-- $ git add 'somefile.file' 
// adds file to the repository from local
4-- git commit -m 'some comment about the changes'
// commits changes to local git repository
5-- git push 
 -- Username [MikeTyphon]
 -- Password [***********]
//sends changes to online git repository
6-- git status
//check to see everything has been properly updated and added to the appropriate areas
7-- git config --global user.name "Mike Typhon"
// sets username for repository (should work for all local git files and folders)
8-- git config --global user.email "alexcgaiser@gmail.com"
// sets email for repository (should work for all local git files and folders)
9-- git config --list
// showgit s all configuration items for depository (should be updated with info in 7--,8--)
10-- git init
// sets up directory to be tracked and manipulated via git
11-- git branch [nameofbranch]
//creates branch
12-- git checkout [nameofbranch]
//switches to that branch asterisk(*) indicates active branch
13-- git branch -a
//shows all branches including active branch

